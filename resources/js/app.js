import './bootstrap'; // Mengimpor konfigurasi dari bootstrap.js (Axios, Echo, dll.)

import Alpine from 'alpinejs';
window.Alpine = Alpine;
Alpine.start();

// Kode JavaScript aplikasi Anda yang lain
console.log('App started, Alpine initialized.');